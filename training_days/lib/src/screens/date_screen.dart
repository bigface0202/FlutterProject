import 'package:flutter/material.dart';

class DateScreen extends StatelessWidget {
  build(context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Test screen'),
      ),
      body: Text('This page is test'),
    );
  }
}
